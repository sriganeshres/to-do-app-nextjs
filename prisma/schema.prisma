// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:hHKPsfOJRctltRJTuKnBlOnDarBjMUvE@roundhouse.proxy.rlwy.net:19061/railway"
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  name      String?
  todos     Todo[]   // One-to-many relationship with todos
}

model Todo {
  id        String   @id @default(uuid())
  title     String
  createdAt DateTime @default(now())
  complete  Boolean  @default(false)
  updatedAt DateTime @updatedAt
  userId    String   // Foreign key to link to User
  user      User     @relation(fields: [userId], references: [id])
  typeId    String   // Foreign key to link to Type
  type      Type     @relation(fields: [typeId], references: [id])
}

model Type {
  id   String @id @default(uuid())
  name String @unique
  todos Todo[] // Relation to Todo model
}



